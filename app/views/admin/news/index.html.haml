%h2{:align => "center"} Listing News
%p{:align => "center"}
  = link_to "Create news", new_admin_news_path, |
    class: "btn btn-large btn-primary"
%ul.article_and_news_table
  %table.table.table-striped
    %tr
      %th Id
      %th{:width => "200px"} Title
      %th{:width => "300px"} Body
      %th Create_at
      %th Actions
    - @news.each do |new|
      %tr
        %td
          = new.id
        %td
          = truncate(new.title, length: 100)
        %td
          = raw(strip_tags(new.body[0..140]+'...'))
        %td
          = new.created_at
        %td
          = link_to 'Show', admin_news_path(new), class: "btn btn-default btn-sm"
          = link_to 'Edit', edit_admin_news_path(new), class: "btn btn-info btn-sm"
          = link_to 'Destroy', admin_news_path(new), class: "btn btn-danger btn-sm", |
            method: :delete,                                                                     |
            data: { confirm: 'Are you sure?' }
  = will_paginate @collection, renderer: BootstrapPagination::Rails
  %br/